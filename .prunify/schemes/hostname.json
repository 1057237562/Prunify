{
  "command": "hostname",
  "version": 1,
  "rules": [
    {
      "action": "keep",
      "match_condition": {
        "type": "Regex",
        "pattern": ".*"
      },
      "description": "Keep all lines — output is always a single hostname line"
    }
  ]
}
