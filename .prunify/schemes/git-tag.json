{
  "command": "git tag",
  "version": 1,
  "rules": [
    {
      "action": "keep",
      "match_condition": {
        "type": "Regex",
        "pattern": ".*"
      },
      "description": "Keep all lines — output is already concise (list of tag names)"
    }
  ]
}
