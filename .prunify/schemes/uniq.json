{
  "command": "uniq",
  "version": 1,
  "rules": [
    {
      "action": "keep",
      "match_condition": {
        "type": "Regex",
        "pattern": ".*"
      },
      "description": "Keep all lines — uniq output should never be pruned"
    }
  ]
}
