{
  "command": "uname -a",
  "version": 1,
  "rules": [
    {
      "action": "keep",
      "match_condition": {
        "type": "Regex",
        "pattern": ".*"
      },
      "description": "Keep all lines — output is always a single kernel info line"
    }
  ]
}
