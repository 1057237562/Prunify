{
  "command": "rmdir",
  "version": 1,
  "rules": [
    {
      "action": "discard",
      "match_condition": {
        "type": "Regex",
        "pattern": ".*"
      },
      "description": "Discard all stdout — rmdir produces no output on success; errors go to stderr"
    }
  ]
}
