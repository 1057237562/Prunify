{
  "command": "git diff",
  "version": 1,
  "rules": [
    {
      "action": "keep",
      "match_condition": {
        "type": "Column",
        "index": 0,
        "pattern": "^[+-]"
      }
    }
  ]
}
